## Makefile

The `Makefile.toml` file defines common tasks for development. To run specific flows (a collection of tasks) or individual tasks use [`cargo-make`](https://github.com/sagiegurari/cargo-make): `cargo make <task>`.

### Lints

### Tests

### Benchmarks

### Profiling

### Releasing